import { WordToComparisonOpPipe } from './word-to-comparison-op.pipe';

describe('WordToComparisonOpPipe', () => {
  it('create an instance', () => {
    const pipe = new WordToComparisonOpPipe();
    expect(pipe).toBeTruthy();
  });
});
